{{#each order}}
    <tr>
        <td class="leftdiz">{{productID.prodName}}</td>
        <td class="text-right" >{{orderQuantity}}</td>
        <td class="text-right" >₱{{productID.prodPrice}}</td>
        <td class="text-right pr-2">₱{{subTotal}}</td>
    </tr>
{{/each}}